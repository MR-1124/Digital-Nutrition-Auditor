[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Tell Netlify to ignore these files during the secret scan to prevent false positives
  # regarding the project name matching the Firebase Project ID.
  SECRETS_SCAN_OMIT_PATHS = "package.json,package-lock.json,DEPLOYMENT.md"
